---
const { tags, currentCategory } = Astro.props;
const currentPath = Astro.url.pathname;
const isAllPosts = currentPath === '/blog' || currentPath === '/blog/';
const isCategory = currentPath.startsWith('/category');
const isTags = currentPath.startsWith('/tags');
const showToggle = isAllPosts || isCategory || isTags;
---

<nav class="blog-nav" aria-label="Archive navigation">
  <div class="nav-content">
    <span class="nav-label">Browse</span>
    <a href="/blog" class={`nav-link ${isAllPosts ? 'active' : ''}`}>All</a>
    <a href="/category" class={`nav-link ${isCategory ? 'active' : ''}`}>Categories</a>
    <a href="/tags" class={`nav-link ${isTags ? 'active' : ''}`}>Tags</a>

    {currentCategory && <span class="item-badge">Category: {currentCategory}</span>}
    {tags && tags.length > 0 && tags.map((t) => <span class="item-badge">#{t}</span>)}
  </div>

  {showToggle && (
    <div class="view-toggle">
      <button id="view-expanded" class="toggle-btn active" title="Expanded view" aria-label="Expanded view">
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18"></rect><path d="M3 12h18"></path></svg>
      </button>
      <button id="view-compact" class="toggle-btn" title="Compact view" aria-label="Compact view">
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M8 6h13M8 12h13M8 18h13M3 6h.01M3 12h.01M3 18h.01"></path></svg>
      </button>
    </div>
  )}
</nav>

<script>
  document.addEventListener('astro:page-load', () => {
    const btnExpanded = document.getElementById('view-expanded');
    const btnCompact = document.getElementById('view-compact');
    const gridContainer = document.querySelector('.post-grid');
    let currentMode = localStorage.getItem('blog_view_mode') || 'expanded';

    function updateClasses(mode) {
      const viewToggle = document.querySelector('.view-toggle');
      viewToggle?.setAttribute('data-mode', mode);

      if (mode === 'compact') {
        gridContainer?.classList.add('compact-view');
        btnCompact?.classList.add('active');
        btnExpanded?.classList.remove('active');
      } else {
        gridContainer?.classList.remove('compact-view');
        btnExpanded?.classList.add('active');
        btnCompact?.classList.remove('active');
      }
    }

    function setMode(mode) {
      if (!gridContainer || mode === currentMode) return;
      gridContainer.style.opacity = '0';
      gridContainer.style.transform = 'translateY(10px)';

      setTimeout(() => {
        updateClasses(mode);
        currentMode = mode;
        localStorage.setItem('blog_view_mode', mode);
        requestAnimationFrame(() => {
          gridContainer.style.opacity = '1';
          gridContainer.style.transform = 'translateY(0)';
        });
      }, 160);
    }

    updateClasses(currentMode);
    btnExpanded?.addEventListener('click', () => setMode('expanded'));
    btnCompact?.addEventListener('click', () => setMode('compact'));
  });
</script>

<style>
  .blog-nav {
    position: sticky;
    top: 0;
    z-index: 50;
    display: flex;
    justify-content: space-between;
    align-items: stretch;
    gap: 1rem;
    margin-bottom: var(--space-xl);
    border: 1px solid var(--border);
    background: color-mix(in srgb, var(--surface) 94%, transparent);
  }

  .nav-content {
    display: flex;
    align-items: center;
    gap: 0;
    min-width: 0;
    overflow-x: auto;
  }

  .nav-label,
  .nav-link,
  .item-badge,
  .toggle-btn {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.13em;
    text-transform: uppercase;
  }

  .nav-label {
    padding: 0.9rem 1rem;
    color: var(--text-tertiary);
    border-right: 1px solid var(--border);
  }

  .nav-link,
  .item-badge {
    display: inline-flex;
    align-items: center;
    min-height: 100%;
    padding: 0.9rem 1rem;
    border-right: 1px solid var(--border);
    white-space: nowrap;
  }

  .nav-link:hover,
  .nav-link.active {
    background: var(--text-primary);
    color: var(--canvas);
  }

  .item-badge {
    color: var(--text-tertiary);
  }

  .view-toggle {
    display: flex;
    border-left: 1px solid var(--border);
  }

  .toggle-btn {
    display: grid;
    place-items: center;
    width: 3rem;
    border: 0;
    border-right: 1px solid var(--border);
    background: transparent;
    color: var(--text-primary);
  }

  .toggle-btn:last-child {
    border-right: 0;
  }

  .toggle-btn:hover,
  .toggle-btn.active {
    background: rgb(var(--accent));
    color: #fffef8;
  }

  @media (max-width: 720px) {
    .blog-nav {
      position: relative;
      flex-direction: column;
      gap: 0;
    }

    .view-toggle {
      border-top: 1px solid var(--border);
      border-left: 0;
    }

    .toggle-btn {
      flex: 1;
      height: 2.8rem;
    }
  }
</style>
